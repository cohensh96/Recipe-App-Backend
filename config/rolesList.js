/**
 * Object that represents a list of roles with their corresponding numeric values.
 * Each role is defined as a key-value pair, where the key is the role name and the value is the numeric value.
 */
const ROLES_LIST = { 
    "User": 80085,
    "Admin": 420420
};

/**
 * Exporting the ROLES_LIST object to make it available for other parts of the codebase.
 * This object provides a mapping between role names and their corresponding numeric values.
 */
module.exports = ROLES_LIST;
